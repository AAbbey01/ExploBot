# Changelog 
10/9/2024: Bot is almost complete, just added regex to remove the requirement of the word "spotted"


# Known Issues
  - Need to check for coords found not in google maps (super silly)
  - Need to check for multi-line spots (some people do this)
  - Need to confirm that bot scoring merge works
  - Add a website hook so people can see scoreboard (not just sheet because only has uids)
  - Figure out auto sort on sheet (probably really simple to do, can even use like O(n^2 sorting alg because low # of people))
  